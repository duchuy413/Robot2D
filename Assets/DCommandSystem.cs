using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DCommandSystem : MonoBehaviour
{
    public DCommandBackward btnBackward;
    public DCommandForward btnForward;
    public DCommandJump btnJump;
    public DCommandRun btnRun;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
